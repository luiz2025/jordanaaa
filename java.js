// Função para exibir a mensagem quando o envelope for clicado
function abrirMensagem() {
    document.getElementById("mensagemContainer").style.display = "block";
    document.getElementById("envelope").style.display = "none"; // Esconde o envelope após o clique
}
